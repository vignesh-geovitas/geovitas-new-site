/**
 * Canonical origin for the deployed site. Single source of truth shared by the
 * root layout's `metadataBase`, the sitemap, and robots — keep those three in
 * agreement by importing this rather than re-typing the URL.
 */
export const SITE_URL = "https://www.geovitas.com";
